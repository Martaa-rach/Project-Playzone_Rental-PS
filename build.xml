<?xml version="1.0" encoding="UTF-8"?>
<project name="ProjectVina" default="default" basedir=".">
    <description>Builds, tests, and runs the project ProjectVina.</description>

    <import file="nbproject/build-impl.xml"/>

    <!-- Contoh override target run -->
    <target name="run" depends="ProjectVina-impl.jar">
        <exec dir="bin" executable="launcher.exe">
            <arg file="${dist.jar}"/>
        </exec>
    </target>
</project>
